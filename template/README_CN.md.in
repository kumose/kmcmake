<!--
  Auto-generated by kmpkg tools
  --------------------------------
  This section/template was generated by kmpkg for reference.
  You may modify it freely to suit your project needs.
  Recommended to keep the structure for consistency across projects.
  version: @TM_KMCMAKE_VERSION@
  date: @CURRENT_DATE@
-->

@CHANGEME@
=============================

[English](./README.md)


@CHANGEME@ 项目说明

## 🛠️ Build

本项目通过 CMake presets 构建。[kmpkg](https://github.com/kumose/kmcmake) **可选**：设置 `KMPKG_CMAKE` 时，
preset 会将其作为 `CMAKE_TOOLCHAIN_FILE`；未设置则为空工具链，可使用系统库或自行管理依赖。

### 0. 准备环境

- Linux (Ubuntu 20.04+ / CentOS 7+ 推荐) / macOS / Windows（推荐 MSVC + Ninja）
- CMake >= 3.25
- GCC >= 9.4 / Clang >= 12 / MSVC（VS 2022+）
- 可选：安装 `kmpkg`（参见 [安装文档](https://kumo-pub.github.io/docs/category/%E6%8C%81%E7%BB%AD%E9%9B%86%E6%88%90----kmpkg)）
- 可选：使用 `--preset=ninja` 时需在 PATH 中提供 `ninja`

### 1. 配置项目(可选, kmpkg)

- 完整的依赖请参见[`kmpkg.json`](kmpkg.json)
- 更新依赖基线请参见[`kmpkg-configuration.json`](kmpkg-configuration.json) 修改
  `default-registry`的`baseline`
- `baseline` 可通过 `git log` 获取最新提交
- 可选：用户可以自行管理依赖，确保 CMake 的 find_package 能正确找到所需库

    - 例如在系统路径或自定义路径安装依赖，并通过 CMAKE_PREFIX_PATH 指定
    - 或在 kmpkg 中声明外部依赖路径，避免重复下载

### 2. 编译项目

在项目根目录执行：

```bash
# Unix Makefiles（默认）→ build/
cmake --preset=default
cmake --build build -j$(nproc)

# Ninja → build-ninja/（需先安装 ninja）
cmake --preset=ninja
cmake --build build-ninja -j$(nproc)
```

自管理依赖（不使用 kmpkg）：保持 `KMPKG_CMAKE` 未设置，仍可用上述 preset，或使用标准 CMake：

```shell
mkdir build
cd build
cmake ..
make -j$(nproc)
```

***注意***

- 项目根目录 `CMakePresets.json` 已提供 `default` 与 `ninja` 预设。

### 3. 运行测试(可选)

在项目根目录执行：

```shell
ctest --test-dir build
# 若使用 ninja preset：
ctest --test-dir build-ninja
```
